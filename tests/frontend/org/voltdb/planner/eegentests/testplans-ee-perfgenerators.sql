CREATE TABLE ALPHA (
    TINY        TINYINT,
    SMALL       SMALLINT,
    INT         INTEGER,
    BIG         BIGINT,
    STR         VARCHAR
);

CREATE TABLE BETA (
    TINY        TINYINT,
    SMALL       SMALLINT,
    INT         INTEGER,
    BIG         BIGINT,
    STR         VARCHAR
);

CREATE TABLE DMLOUTPUT (
    MODIFIED_ROWS  INTEGER
);
